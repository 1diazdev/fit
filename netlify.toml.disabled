[build]
  command = "bun run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18.17.1"

[functions]
  node_bundler = "esbuild"

# Note: Scheduled functions are configured directly in the function files
# See netlify/functions/*.ts for schedule declarations

# Commented out - This redirect was stripping query parameters
# Astro handles routing, doesn't need SPA fallback
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
